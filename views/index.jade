extends layout

block content
  h1= title
  p Welcome to #{title}

  if messages.length 
    ul
      each message in messages
        li #{message.text}
        li #{message.user}
        li #{message.added}

  a(href="/new") New message 
